#! /bin/sh
# must have perms 0755

PORT=$1

cd <%= site_app_root %>
bundle exec thin \
  --config ./config/one_thin_server.conf \
  --environment production \
  --pid <%= site_app_root %>/tmp/pids/thin.${PORT}.pid \
  --port $PORT \
  start
