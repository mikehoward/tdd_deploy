# -*- ruby -*-

set :user, "mike"

role :hosts, "arch"
#role :hosts, "ubuntu"
# role :hosts, 'li371-193.members.linode.com'

namespace :deploy do
  desc "Moc deploy:update"
  task :update do
    system "echo 'mock deloy:update'"
  end
end
